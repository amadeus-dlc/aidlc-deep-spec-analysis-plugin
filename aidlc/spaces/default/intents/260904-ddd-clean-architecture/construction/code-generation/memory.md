<!-- INVARIANT: examples are single-line HTML comments so a fresh template parses to total=0 (MEMORY_EMPTY). Do NOT un-comment or split across lines. t100 guards this. -->
> This file is kept up to date automatically while the stage runs. Add observations at the review step, not by editing here directly.

## Interpretations
<!-- example: 2026-05-29T10:14:32Z — chose REST over GraphQL; the consuming team only needs CRUD, revisit if subscriptions land -->
- 2026-09-04T05:42:35Z — FR8.1 の「解決された Unit 集合がゼロ件」は `resolveBoltDag` の `state === "none"` と同値と読んだ。`parseBoltDag` が空の `units:` ブロックを `malformed` として弾くため `{ state: "ok", units: [] }` は到達不能で、`malformed` は誤りとして表面化させるべきだから、ゼロ件扱いに含めない。
- 2026-09-04T09:00:17Z — 集約の `conformedTo(schema)` は candidate を置いた直後と cross-check を導いた直後の 2 回呼ぶ。旧実装は候補を適合させてから cross-check を導いており、降格した候補は比較から外れる。単一呼出（crossChecked → conformedTo）だと候補が降格する場合に cross-check の byte が変わるため、NFR1 を守る側を採った。schema は値オブジェクトなので 2 回呼んでも I/O は 1 回のまま（FR1.2 は schema file の二重読取を禁じる趣旨）。
- 2026-09-04T09:00:17Z — `findByDirectory` は verify directory が未作成なら空の集約を返し、公開済み `cross-check.json` は寛容に読む（壊れていれば不在扱い）。cross-check は導出物であって比較の入力ではなく、strict にすると壊れた cross-check が finalization を恒久的に塞ぐ。BR2.7 の strict は兄弟 backend 文書に限定した。
- 2026-09-04T09:24:18Z — requirements の Finalizer は design と違い共通の verified 型を返せない（SMT と Quint で verified の形が違い、Quint だけ `method` を持つ）ため、新しい verdict 型を作らず「保存した集約の候補」＝ `VerificationReport` を返して各 interactor が自分の outcome を組む形にした。「verdict は書かれた姿から導く」は維持。lock のファイル名は design の凍結名 `.deep-spec-design-finalization.lock` を既定に残し、requirements は `.deep-spec-finalization.lock` を注入して運用者が取り違えないようにした。
- 2026-09-04T10:35:00Z — aidlc-workflows の全体スイートを基線（HEAD の一時 worktree、6 ファイル／18 assertion が環境要因で赤）と突き合わせ、作業ツリーで新たに赤の 10 ファイル／25 assertion を分類した。「`resolveBoltDag` が `none` ならゼロ Unit」は粗すぎ、DAG 成果物が無くてもレシート・audit・Unit Progress から観測された Unit で per-Unit 継続性を保つ既存の仕組み（t328 no-DAG continuity）と、team 経路の fail-closed（t324）を潰す。FR8.1 の「解決された Unit 集合」は観測された Unit を含めて解釈し、判定を「DAG ∪ 観測 Unit が空のときだけゼロ Unit」に締め直す（分類 C）。プレースホルダ経路を主張する残りは B として、assertion を消さず fixture に DAG を足すか期待をステージレベルへ改める。
- 2026-09-04T09:50:00Z — オーナーの指示「サンドボックス検証いるよ」で、計画の Step 37 に実サンドボックス検証を足した。前回の裁定記録（src/tools 分離）の Evidence と同じ行列：HEAD の一時 worktree から変更前プラグインを build して A へ、作業ツリーから変更後を B へ導入し、intent-e2e.test.ts と同じ手順で intent を作って 10 entry を導入済み harness のディスパッチャから発火、全出力を byte 比較、A への `--update` 経路、`aidlc-plugin-test` の CLEAN、2 回目発火の収束、lock／temp／stale の残留 0、壊れた兄弟と IR unreadable のライブ確認。単体・統合テストの緑だけでは「導入済みツリーから本物のディスパッチャで動く」ことの証明にならない。
- 2026-09-04T05:42:35Z — Testing Contract の `plan_profile.testable_layers` のうち Frontend behavior を落とし、Repository/data access は `design/adapter` の実ファイルシステム永続化、API/endpoint は出荷物の公開面（findings JSON・stdout verdict・golden・7 harness 配布物）に対応づけた。このプロジェクトには UI が無く、DB の代わりに JSON ファイル永続化がある。

## Deviations
<!-- example: 2026-05-29T10:14:32Z — skipped the optional caching layer the stage prose suggested; the dataset is small enough that it adds risk -->
- 2026-09-04T05:42:35Z — 承認済み計画のチェックボックスを実行中に更新しない運用にした。Plan Approval の fingerprint は code-generation-plan.md の byte を含むため、Step 1-2 を `[x]` にした時点で承認が失効し、サブエージェントの委譲が plan-approval-guard に拒否された（実測）。チェックボックスは全波の生成が終わってから一括で更新し、途中経過は memory.md と code-summary.md で追う。stage-protocol.md §4 のチェックボックス即時同期はこの凍結と両立しないため、凍結を優先した。
- 2026-09-04T08:28:03Z — 初回承認版の Wave 4／5（Repository port に `conformedOf`・`storeConformed(report, model)`・`storeConformedWithoutCrossCheck` を置く設計）をオーナー裁定で撤回した。裁定: Repository の語彙は保存・検索・取得・削除だけで、interface はこの語彙にしか依存できない。他の語彙が要るなら集約の設計を見直す。集約は一塊で、可変部（cross-check の有無）は `Option` として集約自身が持ち、Repository のメソッド変種で吸収しない。functional-spec の Decisions 表「Schema conformance owner: Repository の `conformedOf` を維持」と project.md の「`conformedOf` を既存裁定として維持」は本裁定で覆る。置き換えは集約ルート `DesignVerifyDirectory`（`crossCheck: DesignReport | null` を持つ）＋ `FindingsSchema` 値オブジェクト＋ `DesignReport.conformedTo(schema)`、port は `findByDirectory` と `store(aggregate)` のみ。Wave 4／5 で書いた実装は作り直しになる（実装済みの `DirectoryFinalizationLock`・typed failure・Acquirer は流用）。
- 2026-09-04T08:29:43Z — 計画の改訂承認は、初回承認時のソース状態（stage 開始時の source floor）に縛られていて、Wave 1〜5 で動いた作業ツリーでは記録できなかった。同じ段階への `redo` ジャンプで新しい試行を開始し、指紋を取り直してから承認を記録した。成果物の扱いは Modify（`ARTIFACT_REUSED`）。
- 2026-09-04T08:29:43Z — 改訂計画の承認時に、同じ裁定を `requirements` の `VerificationReportRepository` と `refcheck` の `ReferenceCheckReportRepository` にも**この intent で揃える**と人間が決めた（選択肢「今回揃える」）。要件の Out of Scope「requirements／refcheck report repository 全体の横断再設計」より、Repository 一般の語彙についての本裁定を優先する。実装は design と同じ形（集約の `conformedTo(schema)` ＋ port は find と `store(aggregate)` のみ）。
- 2026-09-04T09:40:00Z — この環境（Claude Code）の導入コピー `.claude/tools/aidlc-sensor-traceability.ts` はゼロ Unit 修正前で、この段階の traceability.json に対して `cannot derive the construction unit from output path` で落ちることを実測した（FR8 が直した当の不具合）。要件の制約は `.claude/` を変更対象外としているが、FR8.3 の「現在の Intent を継続するための導入コピー同期」は継続中の harness に当てはまるため、人間の裁定で `.claude/tools/` の 2 ファイル（`aidlc-lib.ts`・`aidlc-sensor-traceability.ts`）だけを `dist/claude` と byte 一致させることにした（Wave 6 の最終再生成の後に実施）。

## Tradeoffs
<!-- example: 2026-05-29T10:14:32Z — picked TDD over BDD this run; the team is unit-first and the domain is well-understood -->
- 2026-09-04T05:42:35Z — 作業ツリーに既にあったゼロ Unit 修正を破棄せず土台として採用した（Q1=A）。7 harness の配布物まで生成済みで、いま動いているこのステージ自体がその修正の上に乗っているため、破棄の損失が作り直しの利得を上回る。
- 2026-09-04T05:42:35Z — 変更前基線を実測して記録した。deep-spec-analysis: tsc 0 エラー、bun test 527 pass / 1 skip / 0 fail（28 ファイル、2,855 assertions、34.30s、カバレッジ床 0.9 通過）。aidlc-workflows: typecheck 0 エラー、package --check 7 harness すべて OK、t281/t320/t68 は 29 pass / 0 fail。aidlc-workflows は node_modules が無く `bun install --frozen-lockfile` が先に要る。

## Open questions
<!-- example: 2026-05-29T10:14:32Z — confirm the retention window with compliance before the next stage hardens the schema -->
- 2026-09-04T07:30:00Z — aidlc-workflows の変更前基線として `bash tests/run-tests.sh` を走らせていなかった。Wave 6 の全体スイートが 16 ファイル・43 assertion 赤で戻ったとき、変更由来か既存かを判定する材料が無く、HEAD の一時 worktree で基線を取り直すことになった。NFR3 の「変更前に全テストの基線を記録する」は、対象リポジトリごとに**全体スイート**で取らないと後で切り分けができない。
- 2026-09-04T12:11:06Z — ゼロ Unit 判定は最終的に「計画が EXECUTE でない、または DAG ∪ 観測 Unit（audit から再構成）∪ ディスク上の `construction/<unit>/<stage>/` が空」と定義した。観測 Unit は `reviewAttemptWindow` 経由だと判定自身を呼んで再帰するため、同じプリミティブ（`readAuditShardEvents`・`boltEventUnits`）で floor 非依存に読む。Unit Progress は有効な DAG を前提に derive されるので独立の観測源にはならない。
- 2026-09-04T12:11:06Z — aidlc-workflows のテストは、単独の `bun test <file>` とランナー `run-tests.sh` で結果が変わるものがある（`t186`・`t188` は単独だと HUMAN_TURN 台帳や質問ファイル前提で赤、ランナー経由では緑）。回帰かどうかは全体スイートかランナーの `--filter` で判定し、単独実行の赤だけで判断しない。
- 2026-09-04T10:24:47Z — サンドボックスの「壊れた兄弟」確認で、センサーが exit 1 で公開を止めても、上流ディスパッチャ `aidlc-workflows/core/tools/aidlc-sensor.ts` の凍結真理値表（冒頭 20-28 行、`status non-0/non-127 → PASSED script-error: exit-<n>`）は verdict 行を `"result":"passed","note":"script-error: exit-1"` と表示する。プラグインは verified を返していないので FR1.3 は満たすが、advisory の語が `passed` に見える。今回の変更ではなく、aidlc-workflows 側の後続候補。
- 2026-09-04T07:30:00Z — `DesignMachines.attrPathOf(sm)` はファーストクラスコレクションの static が `sm.entity()` / `sm.attribute()` を外から読んでおり Tell-Don't-Ask に反する。`DesignMachine.attrPath()` へ移すのが素直だが `design-event-catalog.ts` と `verify-design-quint-usecase.ts:150` へ波及するため今 intent では触っていない。既存の逸脱であって今回持ち込んだものではない。
